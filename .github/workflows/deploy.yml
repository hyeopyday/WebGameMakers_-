name: Deploy Vite → Nginx (via ~/frontEnd)

on:
  push:
    branches: [ "main" ]

jobs:
  deploy-vite:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install & Build (Vite)
        run: |
          npm ci
          npm run build   # => dist/

      - name: Prepare SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.RASP_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "${{ secrets.RASP_HOST }}" >> ~/.ssh/known_hosts

      # 1) 원격: ~/frontEnd 를 매번 초기화
      - name: Clean ~/frontEnd on server
        run: |
          PORT="${{ secrets.RASP_PORT }}"
          ssh -p "$PORT" ${{ secrets.RASP_USER }}@${{ secrets.RASP_HOST }} '
            set -e
            TARGET="$HOME/frontEnd"
            rm -rf "$TARGET" && mkdir -p "$TARGET"
          '

      # 2) dist → 원격의 ~/frontEnd 로 업로드
      - name: Upload dist → ~/frontEnd
        run: |
          rsync -az --delete -e "ssh -p ${{ secrets.RASP_PORT }}" ./dist/ \
            ${{ secrets.RASP_USER }}@${{ secrets.RASP_HOST }}:~/frontEnd/

      # 3) ~/frontEnd 내용을 Nginx 루트로 반영(+ 재시작)
      - name: Deploy to Nginx root & reload
        run: |
          PORT="${{ secrets.RASP_PORT }}"
          # 🔧 필요하면 여기 경로만 바꿔: /var/www/html 또는 /var/www/viteapp 등
          NGINX_ROOT="/var/www/frontEnd"

          ssh -p "$PORT" ${{ secrets.RASP_USER }}@${{ secrets.RASP_HOST }} "
            set -e
            sudo mkdir -p \"$NGINX_ROOT\"
            sudo rsync -a --delete \"\$HOME/frontEnd/\" \"$NGINX_ROOT/\"
            sudo systemctl reload nginx || sudo systemctl restart nginx
          "
